```
1.面向机器： 程序 = 数据+指令
2.面向过程： 程序 = 数据结构+算法
3.面向对象： 程序 = 对象+消息 
```

```
抽象：abstract （不能实例化，只能被继承，可以申明，只能创建子类）
对象：状态、行为
消息：
类：程序由类构成
继承：
多态：
封装：
```

```java
类和对象
类是一种对象的设计图纸
对象是类的实例
class A{
    int i ;
    void aaa(){
        
    }
}

A aobj = new A();
aobj.aaa();
```

```
类定义格式：
【修饰符关键字】 class 类名 【extends 父类】【implements 接口列表】
{
    //类体的内容
}
所有的类都是Object的子类
修饰符：
public
abstract
final 不能被继承

类的主要成员：
变量成员：属性
方法成员：行为
变量成员的定义格式：
【修饰符关键字】 类型 变量名;

方法成员的定义格式：
【修饰符关键字】 返回类型 方法名（【参数列表：类型 参数名,类型 参数名】）{
    //方法体
}

```

